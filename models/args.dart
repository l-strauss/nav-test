class FirstScreenArguments {
  final String title;
  final String message;

  FirstScreenArguments(this.title, this.message);
}

class SecondScreenArguments {
  final String title;
  final String message;

  SecondScreenArguments(this.title, this.message);
}
